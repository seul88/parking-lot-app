'use client'

import BarLoader from "react-spinners/BarLoader";

export const CustomBarLoader = () => {
    return (
        <BarLoader 
            width={180}
            height={20}
            color={'#D3D3D3'}
        />
    );
};